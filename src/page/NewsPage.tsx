import { Box } from '@mui/material';
import classNames from 'classnames';
import React, { useEffect, useRef } from 'react';

const NewsPage = () => {
  return (
    <>
      <div className={classNames(`col-span-9`)}></div>
    </>
  );
};

export default NewsPage;
